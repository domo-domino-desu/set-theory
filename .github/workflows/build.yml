name: Build and release PDF

on:
  workflow_dispatch:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: write

concurrency:
  group: pdf-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    container: texlive/texlive:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Trust the workspace
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global --add safe.directory "$GITHUB_WORKSPACE/OpenLogic"

      - name: Build the Chinese edition with LuaLaTeX
        run: make settheory-screen.pdf settheory-print.pdf settheory-print-cover.pdf

      - name: Report build failure
        if: failure()
        run: |
          {
            echo '```text'
            tail -n 120 settheory-screen.log 2>/dev/null || true
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Package PDFs
        run: |
          mkdir -p dist
          cp settheory-screen.pdf dist/set-theory-zh-screen.pdf
          cp settheory-print.pdf dist/set-theory-zh-print.pdf
          cp settheory-print-cover.pdf dist/set-theory-zh-print-cover.pdf

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: set-theory-zh
          path: dist/*.pdf

      - name: Release
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: 集合论 Build ${{ github.run_number }}
          files: dist/*.pdf
          generate_release_notes: true
